{
  "name": "dlsite_title_copy",
  "version": "1.0.0",
  "description": "DLSiteのタイトルをコピーできるようにするやつ",
  "main": "main.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
